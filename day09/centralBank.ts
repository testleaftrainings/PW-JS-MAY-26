// class ClassName{}
export interface RBI{  
// STRICT ABSTRACTION? every properties should be hidden

// you are allowed to create method without the implementation / {body}
// to create a contract
repoRate():void;
kycMandatory():void;
upiPayment():void;
}

// in interface we can have a constructor , we can create an object for the interface